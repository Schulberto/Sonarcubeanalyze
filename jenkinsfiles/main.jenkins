#!groovy
properties([disableConcurrentBuilds()])
pipeline {
        agent any
        stages {
stage   ('Code analyze with Sonarcube')
{
  def scannerhome = tool 'scanner';
  withSonarQubeEnv ('SonarOUT') 
  {
    sh "${tool("scanner")}/bin/sonar-scanner"
    // some block
    }
  }
}
stage ('Build')
	{
		script
		{
			echo '----BUILD----'
			sh 'pwd'
		}
	}
}
